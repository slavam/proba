# coding: utf-8
<h1></h1>

<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @city_types.each do |ct| %>
  <tr>
    <td><%= ct.id %></td>
    <td><%= ct.full_name %></td>
    <td><%= ct.short_name %></td>
    <td><%= link_to 'Show', ct %></td>
    <td><%= link_to 'Edit', edit_city_type_path(ct) %></td>
    <td><%= link_to 'Destroy', ct, :confirm => 'Are you sure?', :method => :delete, :remote=>true, :class=>'delete_post' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_city_type_path %>


